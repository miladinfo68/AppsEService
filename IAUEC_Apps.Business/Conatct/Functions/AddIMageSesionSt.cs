using System;
using System.Data;
using System.IO;

namespace IAUEC_Apps.Business.Conatct.Functions
{
    public  class AddIMageSesionSt
    {
        #region Function
       // public static  DataTable FnDtAddIMageSesionSt(DataTable dt, string id)
       // {

       //    const string imageContact = "../img/avatarContact.jpg";
       // //   const string imageUser =

       ////     byte[] b = File.ReadAllBytes(imageUser);

       //     if (dt != null && dt.Rows.Count > 0)
       //     {
       //         dt.Columns.Add("Image");
       //         for (int i = 0; i < dt.Rows.Count; ++i)
       //         {
       //             if ((dt.Rows[i]["ID_Sender"].ToString()) == id)
       //             {
       //                 dt.Rows[i]["Image"] = imageContact;
       //             }
       //             else
       //                 dt.Rows[i]["Image"] = imageContact;

       //         }
       //     }
       //     return dt;
       // }
        #endregion

    }
}
